# StarWarsApp
A lightweight full-stack app that pulls data from the Star Wars API (SWAPI) and exposes it through a custom backend endpoint, then renders it in a simple, responsive frontend interface.
